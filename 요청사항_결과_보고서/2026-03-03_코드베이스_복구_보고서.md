# 코드베이스 복구(리셋) 수정사항 보고서

## 요청 정보

- **날짜**: 2026-03-03
- **요청 내용**: 지정 커밋 `da5ed4f416c864c45f3c6edb7b00cc3d31dd135a` 상태로 복구.  
  (APK로 테스트한 앱에서는 이전 손글씨/텍스트가 보이는데, Cursor 연결 테스트 앱에서는 안 보이는 차이가 있어, 해당 시점 코드 상태로 되돌림.)

## 진행 내용

### 1) 복구 대상 커밋 확인

- 커밋: `da5ed4f416c864c45f3c6edb7b00cc3d31dd135a`
- 메시지: `feat: 툴바 선택·자유형 선택(미디어/텍스트)·텍스트/빠른텍스트 커서 (2026-02-12)`

### 2) Git Hard Reset 수행

- 저장소 루트(`/ink`)에서 아래 명령 실행.
- `git reset --hard da5ed4f416c864c45f3c6edb7b00cc3d31dd135a`
- 결과: HEAD가 해당 커밋으로 이동, 이후 커밋 및 로컬 수정 사항 전부 제거됨.

## 결과

| 항목 | 상태 |
|------|------|
| HEAD 복구 | ✅ `da5ed4f` (2026-02-12 툴바/선택/텍스트 커서 커밋) |
| 이후 커밋 제거 | ✅ 1개 커밋(c5cf7c6 등) 제거 |
| 추적 중이던 수정 파일 | ✅ 해당 커밋 기준 상태로 복원 |

## 복구로 제거된 변경 사항 요약

(리셋으로 사라진, 그동안 적용했던 수정들)

- 디버그 모드에서 캔버스 Firestore 초기 로드량 축소(테스트 시 Read 절감)
- 채팅 목록 즉시 표시: RoomProvider 방 목록 먼저 notify, 멤버 정보 백그라운드 로드
- 채팅 목록 1회 조회: `getRoomsOnce()` 추가 및 `initialize()`에서 선 로드
- Firestore 1회 로드 직후 캔버스 즉시 그리기(`_notifyIfNotDisposed()` 호출)
- tags_tab: 미사용 `_getRoomById` 제거, `room_model` import 제거
- 워크플로 오케스트레이션 문서 추가 및 Cursor 규칙에 “항상 참고” 반영
- INK 최종 기능 명세서: 저장 용량·구독·보관 정책(섹션 15) 추가
- RTDB_SETUP.md 보강, RTDB 규칙 배포 안내 등

위 항목들은 현재 코드베이스에는 **포함되어 있지 않음**(da5ed4f 시점으로 되돌린 상태).

## 변경된 파일

- **복구(리셋)**: Git으로 관리되는 모든 파일이 `da5ed4f` 내용으로 일괄 복원됨.
- **추적되지 않은 파일(유지됨)**  
  - `ink_talk/docs/RTDB_SETUP.md`  
  - `잉크_앱프로젝트_정보/워크플로_오케스트레이션.md`  

  위 두 파일은 해당 커밋에 포함되지 않았던 미추적 파일이라 리셋 후에도 그대로 남아 있음.

## 관련 커밋

- **복구 대상**: `da5ed4f416c864c45f3c6edb7b00cc3d31dd135a`
- **커밋 메시지**: feat: 툴바 선택·자유형 선택(미디어/텍스트)·텍스트/빠른텍스트 커서 (2026-02-12)

## 테스트 방법

- `flutter run` 또는 기기/에뮬레이터에서 앱 실행.
- 채팅방 입장 후, 이전에 저장된 손글씨/텍스트가 APK 테스트 환경과 동일한 조건에서 보이는지 확인.

## 알려진 제한사항 / TODO

- 리셋으로 제거된 기능(채팅 즉시 표시, 디버그 Read 절감, RTDB 실패 시 Firestore 먼저 그리기 등)이 필요하면 해당 커밋 이후 변경을 선택적으로 다시 적용해야 함.
- RTDB Permission denied가 발생하면 `ink_talk`에서 `firebase deploy --only database`로 규칙 배포 필요(문서: `ink_talk/docs/RTDB_SETUP.md`).

## 참고 문서

- `요청사항_결과_보고서/2026-02-12_오늘_수정_추가_보고서.md` (복구 기준이 된 시점 작업)
- `ink_talk/docs/RTDB_SETUP.md` (RTDB 규칙 배포 및 Permission denied 대응)
